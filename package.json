{
  "name": "oh-my-apps",
  "private": true,
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "pretest": "eslint packages",
    "test": "jest"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
